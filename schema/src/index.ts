// Resume data schema — field-level shape is not yet decided
// (see "Open / deferred" in docs/architecture-decisions.md, decision 5).
// This package exists so `pipeline` and `frontend` have a shared workspace
// dependency to import types from once the schema is defined.
export {};
