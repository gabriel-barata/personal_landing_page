# Google Cloud service account credentials JSON (single-line). The target
# Google Doc must be shared with this service account's email as a viewer.
# See docs/architecture-decisions.md, decision 4.
GOOGLE_SERVICE_ACCOUNT_KEY=

# The Google Doc ID to fetch (the id segment of the doc's URL).
GOOGLE_DOC_ID=
